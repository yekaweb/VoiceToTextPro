<UserControl x:Class="VoiceToTextPro.Controls.GlowWaveformVisualizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="600">
    <Grid Background="Transparent">
        <!-- Background Ambient Glow -->
        <Ellipse x:Name="AmbientGlow" 
                 Width="250" Height="50" 
                 HorizontalAlignment="Center" 
                 VerticalAlignment="Center" 
                 Opacity="0.35">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#8B5CF6" Offset="0"/>
                    <GradientStop Color="#0006B6D4" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <!-- Waveform Canvas -->
        <Canvas x:Name="WaveCanvas" ClipToBounds="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
