<UserControl x:Class="VoiceToTextPro.Tabs.TTSTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/GlassmorphismResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header Title Banner -->
            <RowDefinition Height="*"/>    <!-- Main Working Grid -->
        </Grid.RowDefinitions>

        <!-- ═══ HEADER BANNER ═══ -->
        <Border Grid.Row="0" Style="{StaticResource MacGlassCardStyle}" Padding="18,14" Margin="0,0,0,14">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="12" Background="#25D946EF" Margin="0,0,14,0">
                        <TextBlock Text="🗣️" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource Tab_TTS_Title}" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF"/>
                        <TextBlock Text="{DynamicResource Tab_TTS_SubTitle}" FontSize="12" Foreground="#94A3B8" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="ImportSubtitleTextBtn" Content="{DynamicResource TTS_ImportSubText}" Style="{StaticResource MacPillButtonStyle}" Click="ImportSubtitleTextBtn_Click" Margin="0,0,10,0"/>
                    <Button x:Name="RefreshModelsBtn" Content="🔄" Width="36" Height="36" Style="{StaticResource MacGhostButtonStyle}" Click="RefreshModelsBtn_Click" ToolTip="بروزرسانی لیست مدل‌ها"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══ MAIN WORKING GRID (Left: Controls & Settings | Right: Text Input & Audio Player) ═══ -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/> <!-- Left Sidebar: Model & Voice Settings -->
                <ColumnDefinition Width="*"/>   <!-- Right Main Area: Text Editor & Audio Player -->
            </Grid.ColumnDefinitions>

            <!-- ─── LEFT PANEL: SETTINGS & MODEL SELECTOR ─── -->
            <Border Grid.Column="0" Style="{StaticResource MacGlassCardStyle}" Padding="18" Margin="0,0,14,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="🎙️ انتخاب صدای گوینده (Piper)" FontSize="14" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,0,10"/>
                        
                        <!-- Model Dropdown -->
                        <ComboBox x:Name="VoiceModelComboBox" Height="38" Margin="0,0,0,16" DisplayMemberPath="Name" Background="#1E2736" Foreground="#FFFFFF"/>

                        <Border Height="1" Background="#20FFFFFF" Margin="0,0,0,16"/>

                        <!-- Speech Speed Slider -->
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="⚡ سرعت گفتار" Foreground="#CBD5E1" FontWeight="Medium" FontSize="13"/>
                            <TextBlock x:Name="SpeedValueText" Text="1.0x" Foreground="#D946EF" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Slider x:Name="SpeedSlider" Minimum="0.5" Maximum="2.0" Value="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" ValueChanged="SpeedSlider_ValueChanged" Margin="0,0,0,20"/>

                        <!-- Speaker ID (For Multi-speaker models) -->
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="🎭 شناسه گوینده (Speaker ID)" Foreground="#CBD5E1" FontWeight="Medium" FontSize="13"/>
                            <TextBlock x:Name="SpeakerIdText" Text="0" Foreground="#A855F7" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Slider x:Name="SpeakerSlider" Minimum="0" Maximum="10" Value="0" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SpeakerSlider_ValueChanged" Margin="0,0,0,20"/>

                        <Border Height="1" Background="#20FFFFFF" Margin="0,0,0,16"/>

                        <!-- Action Button: Synthesize Speech -->
                        <Button x:Name="SynthesizeBtn" Content="⚡ سنتز و تولید صدا" Style="{StaticResource MacPillButtonStyle}" Height="46" FontSize="14" FontWeight="Bold" Click="SynthesizeBtn_Click"/>

                        <!-- Status Log -->
                        <TextBlock x:Name="StatusLogText" Text="آماده سنتز گفتار" Foreground="#94A3B8" FontSize="12" Margin="0,14,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- ─── RIGHT PANEL: TEXT EDITOR & AUDIO PLAYER ─── -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>    <!-- Text Editor Area -->
                    <RowDefinition Height="Auto"/> <!-- Audio Player Bar -->
                </Grid.RowDefinitions>

                <!-- Text Editor Card -->
                <Border Grid.Row="0" Style="{StaticResource MacGlassCardStyle}" Padding="18" Margin="0,0,0,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Top Editor Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Text="📝 متن ورودی جهت خوانش" FontSize="14" FontWeight="Bold" Foreground="#E2E8F0"/>
                            <Button x:Name="ClearTextBtn" Content="🗑️ پاک‌سازی" HorizontalAlignment="Right" Style="{StaticResource MacGhostButtonStyle}" Click="ClearTextBtn_Click"/>
                        </Grid>

                        <!-- Input TextBox -->
                        <TextBox x:Name="InputTextBox" Grid.Row="1" 
                                 Background="#1A2030" Foreground="#F1F5F9"
                                 BorderBrush="{StaticResource MacGlassBorderBrush}" BorderThickness="1"
                                 AcceptsReturn="True" 
                                 TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto" 
                                 FontSize="14" 
                                 Padding="12"
                                 TextChanged="InputTextBox_TextChanged"/>

                        <!-- Bottom Counter -->
                        <TextBlock x:Name="CharCounterText" Grid.Row="2" Text="تعداد کاراکتر: ۰" Foreground="#64748B" FontSize="12" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                    </Grid>
                </Border>

                <!-- Audio Player Bar Card -->
                <Border Grid.Row="1" Style="{StaticResource MacGlassCardStyle}" Padding="16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- Play/Pause Button -->
                            <ColumnDefinition Width="*"/>    <!-- Seek Slider & Time -->
                            <ColumnDefinition Width="Auto"/> <!-- Export Button -->
                        </Grid.ColumnDefinitions>

                        <Button x:Name="PlayPauseBtn" Grid.Column="0" Content="▶️" Width="44" Height="44" Style="{StaticResource MacPillButtonStyle}" FontSize="16" Click="PlayPauseBtn_Click" Margin="0,0,14,0"/>

                        <Grid Grid.Column="1" VerticalAlignment="Center" Margin="0,0,14,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Slider x:Name="AudioSeekSlider" Grid.Row="0" Minimum="0" Maximum="100" Value="0" ValueChanged="AudioSeekSlider_ValueChanged"/>
                            <Grid Grid.Row="1" Margin="0,4,0,0">
                                <TextBlock x:Name="CurrentTimeText" Text="00:00" Foreground="#CBD5E1" FontSize="11" HorizontalAlignment="Left"/>
                                <TextBlock x:Name="TotalTimeText" Text="00:00" Foreground="#64748B" FontSize="11" HorizontalAlignment="Right"/>
                            </Grid>
                        </Grid>

                        <Button x:Name="ExportWavBtn" Grid.Column="2" Content="💾 ذخیره WAV" Style="{StaticResource MacGhostButtonStyle}" Height="40" Click="ExportWavBtn_Click"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
